# Upstream baseline

This SDK copy is based on:

- Repository: `https://github.com/agilexrobotics/ugv_sdk.git`
- Commit: `f2704eacdc90357078cd93ec60aae08bb4baab35`
- CMake project version: `0.8.0`

The ROS package manifest is adapted locally so colcon treats the SDK as a
plain CMake package instead of a ROS 1 catkin package.
